apiVersion: rbac.authorization.k8s.io/v1

kind: RoleBinding

metadata:

  name: pod-reader-binding

  namespace: rbac-lab

subjects:

- kind: ServiceAccount

  name: dev-user

  namespace: rbac-lab

roleRef:

  kind: Role

  name: pod-reader

  apiGroup: rbac.authorization.k8s.io